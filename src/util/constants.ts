export enum StatusPost {
  Process = "Process",
  Approved = "Approved",
  Rejected = "Rejected",
}

export enum KEY {
  ACCESS_TOKEN = "accessToken",
}

export enum AlertStatus {
  SUCCESS = "success",
  ERROR = "error",
  INFO = "info",
}
